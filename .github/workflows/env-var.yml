name: env-var
on:
  workflow_dispatch:
env:
  APP_NAME: myapp

jobs:
  env-jobs:
    runs-on: self-hosted
    env:
      ENVIRONMENT: staging

    steps:
      - name: step-jobs
        env:
          VERSION: 1.0.0
        run: echo $APP_NAME && echo $ENVIRONMENT && echo $VERSION
    
